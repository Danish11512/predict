# Optional: used when you run `bun dev` directly (run.sh exports these from backend/.env).
# development | production — run.sh only frees ports when APP_ENV is not production.
APP_ENV=development
BACKEND_PORT=8000
FRONTEND_PORT=5173
