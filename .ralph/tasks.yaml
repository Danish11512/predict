# Ralph Task Cache
# Auto-generated from RALPH_TASK.md
# DO NOT EDIT - regenerated on task file change

source_file: RALPH_TASK.md
source_mtime: 1775620669
generated_at: 2026-04-08T04:02:02Z

tasks:
  - id: "line_31"
    line_number: 31
    status: pending
    parallel_group: 999999
    description: **Payload contract:** `GET /live-games` and SSE `event: data` still return `{\"updated_utc\", \"games\"}`. Each element remains `{ \"scraped\", \"event\", \"markets\" }` at minimum; **add** documented optional keys (e.g. `milestone`, `live_data`, `game_stats`, `markets_enriched` or in-place extended `markets` entries) without breaking the frontend’s current readers (backward compatible or version behind a flag documented in code comments).
    indent_level: 0
    raw_marker: 1.
  - id: "line_32"
    line_number: 32
    status: pending
    parallel_group: 999999
    description: **Parity with scrape:** For a given scrape-derived list, the set of **event tickers** (and market tickers from href) matches the existing `live_games_enrich` dedupe rules; add automated test(s) that mock scrape rows and assert parity.
    indent_level: 0
    raw_marker: 2.
  - id: "line_33"
    line_number: 33
    status: pending
    parallel_group: 999999
    description: **REST baseline:** Implement authenticated Trade API reads (RSA headers per [Quick Start: Authenticated Requests](https://docs.kalshi.com/getting_started/quick_start_authenticated_requests)) for milestones, events with nested markets, single markets when needed, historical cutoff, live data, and game stats. Use **cursor pagination** on all list endpoints, **Retry-After / exponential backoff** on 429, and a **documented max pages / budget** per poll cycle to avoid stalls.
    indent_level: 0
    raw_marker: 3.
  - id: "line_34"
    line_number: 34
    status: pending
    parallel_group: 999999
    description: **WebSocket deltas:** Maintain a WS connection in the backend process; subscribe to tickers derived from the current live snapshot; merge ticker / orderbook / trade deltas into the same snapshot used for SSE pushes.
    indent_level: 0
    raw_marker: 4.
  - id: "line_35"
    line_number: 35
    status: pending
    parallel_group: 999999
    description: **Prices & payout fields:** Every emitted market includes the price rule above and binary payout derived fields; scalar uses `settlement_value_dollars` only post-determination (`Market.status` / `result` as per API).
    indent_level: 0
    raw_marker: 5.
  - id: "line_36"
    line_number: 36
    status: pending
    parallel_group: 999999
    description: **Multivariate:** Confirmed excluded from fetch paths for v1 (no combo events in the primary list).
    indent_level: 0
    raw_marker: 6.
  - id: "line_37"
    line_number: 37
    status: pending
    parallel_group: 999999
    description: **`.env.example`:** Documents `KALSHI_API_KEY_ID`, `KALSHI_PRIVATE_KEY_PATH`, production `KALSHI_TRADE_API_BASE`, and optional `KALSHI_WS_URL` if non-default.
    indent_level: 0
    raw_marker: 7.
  - id: "line_38"
    line_number: 38
    status: pending
    parallel_group: 999999
    description: **Verification:** All of the following pass with no new failures: `cd backend && python3 -m unittest discover -s tests -v`, `cd frontend && bun run check`, `cd frontend && bun run build`.
    indent_level: 0
    raw_marker: 8.
